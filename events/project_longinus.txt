namespace = project_longinus
event = {
	id = project_longinus.1
	diplomatic = yes
	custom_gui = "project_longinus"	#调用一个不存在的GUI使游戏崩溃
	is_triggered_only = yes
	trigger = {
	}
	immediate = {
	}
}

fleet_event = {
	id = project_longinus.2
	title = project_longinus.2.name
	desc = project_longinus.2.desc
	location = from	#A scope to the object that is relevant to the event that player can move to. For example, the planet where event is happening
	is_triggered_only = yes	#The game will not consider this event for starting on its own. Event will need to be triggered by another event, or by some other effect. Most events will use this.
	archaeology = yes
	trigger = {
	}
	immediate = {
		from = {
			set_site_progress_locked = yes
		}
	}
	after = {
		from = {
			set_site_progress_locked = no
		}
	}
	option = {
		name = project_longinus.2.option
		trigger = {
		}
		hidden_effect = {
		}
	}
}

fleet_event = {
	id = project_longinus.3
	title = project_longinus.3.name
	desc = project_longinus.3.desc
	location = from	#A scope to the object that is relevant to the event that player can move to. For example, the planet where event is happening
	is_triggered_only = yes	#The game will not consider this event for starting on its own. Event will need to be triggered by another event, or by some other effect. Most events will use this.
	archaeology = yes
	trigger = {
	}
	
	immediate = {
		from = {
			set_site_progress_locked = yes
		}
	}
	after = {
		from = {
			set_site_progress_locked = no
		}
	}
	option = {
		name = project_longinus.3.option
		trigger = {
		}
		hidden_effect = {
		}
	}
}

fleet_event = {
	id = project_longinus.4
	title = project_longinus.4.name
	desc = project_longinus.4.desc
	location = from	#A scope to the object that is relevant to the event that player can move to. For example, the planet where event is happening
	is_triggered_only = yes	#The game will not consider this event for starting on its own. Event will need to be triggered by another event, or by some other effect. Most events will use this.
	archaeology = yes
	trigger = {
	}
	immediate = {
		from = {
			set_site_progress_locked = yes
		}
	}
	after = {
		from = {
			set_site_progress_locked = no
		}
	}
	option = {
		name = project_longinus.4.option
		trigger = {
		}
		hidden_effect = {
		}
	}
}

fleet_event = {
	id = project_longinus.5
	title = project_longinus.5.name
	desc = project_longinus.5.desc
	location = from	#A scope to the object that is relevant to the event that player can move to. For example, the planet where event is happening
	is_triggered_only = yes	#The game will not consider this event for starting on its own. Event will need to be triggered by another event, or by some other effect. Most events will use this.
	archaeology = yes
	trigger = {
	}
	immediate = {
	}
	option = {
		name = project_longinus.5.option
		trigger = {
		}
		owner = {
			add_relic = r_project_longinus
			add_research_option = tech_reality_distortion_system
		}
	}
}

country_event = {
	id = project_longinus.6
	hide_window = yes
	trigger = {
		
	}
	is_triggered_only = yes
	immediate = {
		random_planet_within_border = {
			limit = {
				NOT = {
					exists = archaeological_site
				}
			}
			create_archaeological_site = project_longinus 
		}
		set_global_flag = project_longinus_exist
	}
}

event = {
	id = project_longinus.7
	hide_window = yes
	trigger = {
		has_global_flag = project_longinus_on
	}
	is_triggered_only = yes
	immediate = {
		every_country = {
			limit = {
				is_ai = no
			}
			country_event = {
				id = project_longinus.8
			}
		}
		observer_event = {
			id = observer.1
		}
	}
}

country_event = {
	id =  project_longinus.8
	title =  project_longinus.8.name
	desc =  project_longinus.8.desc
	auto_opens = yes 
	picture = GFX_evt_DaWuJiangTu #a name of a picture to display
	is_triggered_only = yes 
	trigger = {
	}
	immediate = {
		country_event = {
			id = project_longinus.1
			days = 3
		}
	}
	option = {
		name = project_longinus.8.1
		trigger = {
		}
		custom_tooltip = project_longinus.8.1.tooltip
		hidden_effect = {
			country_event = {
				id = project_longinus.1
			}
		}
	}
}

country_event = {
	id =  project_longinus.9
	title =  project_longinus.9.name
	desc =  project_longinus.9.desc
	picture = GFX_evt_vortex #a name of a picture to display
	is_triggered_only = yes 
	trigger = {
	}
	immediate = {
	}
	option = {
		name = project_longinus.9.1
		remove_relic = r_project_longinus
	}
}